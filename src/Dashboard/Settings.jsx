import React from 'react';
import '../styles/dashboard.css';

const Settings = () => {
  return (
    <div className="dashboard-section">
      <h2>Settings</h2>
      <p>Configure user preferences, notifications, and business details.</p>
      {/* Add settings form later */}
    </div>
  );
};

export default Settings;